# STATUS: chernivtsi-stops-editor

Обновлено: 2026-09-18. Это точка входа для новой сессии (человек или ИИ):
что развёрнуто, что уже сделано, что проверять и что осталось.

## 1. Что где крутится

| Что | Где | Как поднять |
|---|---|---|
| Эмулятор + API (прод) | `https://<домен>/` через nginx (Basic Auth, TLS); контейнер `api_router` слушает только `127.0.0.1:8000` | nginx: `deploy/setup-https.sh` / `deploy/README_HTTPS.md`; деплой: `git pull` → `docker compose up -d --build` |
| UI | `/ui/emulator.html` (эмулятор), `/ui/admin.html` (сленг + жалобы), `/docs` (Swagger) | — |
| Локальный dev | `0.0.0.0:8000` | `cd chernivtsi-stops-editor && python main.py` |
| Тесты | локально | `python -m pytest -q` (сейчас 18 passed) |

Доступ наружу — только через nginx: порт 8000 забинджен на `127.0.0.1`
(`docker-compose.yml`), конфиги и инструкция — `deploy/` (см. открытые задачи).

## 2. Что уже сделано (история коммитов)

Всего ~57 коммитов, репозиторий начат 2026-08-18 (точное число — `git rev-list --count HEAD`).
Ключевое по темам:

**Каркас эмулятора** (`f1749fb`, `f894b26`, `8fcf3de`, `87cb222`, `abdec38`):
живой GPS-слой + граф маршрутов, роутер с 0–2 пересадками и `/api/plan`,
виртуальный парк `sim_layer` (`GPS_SIMULATOR=1`) для тестов в любое время суток,
детерминированные цвета маршрутов, `/api/plan` и `/api/live` работают в рамках
одного модельного времени.

**Оптимизация роутера** (`docs/REVIEW-router-perf.md`):
`cb17ad0` — бриф на оптимизацию; `3d656a1` — Шаг 1, индекс парка `_live_by_route`;
`337b740` — Шаг 2, мемоизация ожиданий внутри `plan()`; `de2c21c` — Шаг 3, отсев
за градусами до haversine; `425d45f` — итог: `plan()` p50 2688 → 89 мс (×30),
end-to-end 1.77 → 0.22 с; `bfbac2d` — pytest-каркас с фиксированным `now`.

**Корректность времени** (`docs/REVIEW-router-correctness.md`):
`ecad622` — Шаги 1–2, ожидание считается на момент прихода пассажира;
`2cd903b` — Шаг 3, «первый нужный ТС» обязан приезжать ПОСЛЕ пассажира
(`BOARD_TOLERANCE_MIN`); `d309093` — разбор результата, end-to-end 0.32 с.

**Карта** (`docs/REVIEW-router-map-paths.md`):
`b7f4a9b` — фикстура закрепляет `snapshot_at=now` (без неё HEAD был красный);
`5eeea8f` — `leg.path` = непрерывный ланцюжок остановок маршрута (был хордой из
2 точек → «немає прорисованих маршрутів»). `path` — это остановки, не геометрия.

**Латентность** (`docs/BRIEF-router-latency.md`, `docs/REVIEW-router-latency.md`):
`1e16f98` — бриф Gemini (Шаг 4 + гигиена); `8f89da0` — Шаг 4: `_approaching_vehicles()`
и `_spatial_cache`, кэш геометрии помечен версией среза и подписью парка
(haversine 33 168 → 133, прод-цикл p50 230 → 76 мс, 6/6 эталонных планов
byte-identical); `38ccdac` — разбор с метриками и правками к брифу.

**Живые ТС на карте** (`b51b7e0`): `heading_deg` (симулятор считает из цепочки
маршрута, реальный GPS — из `orientation`) пошёл в дело: маркеры ТС стали стрелками
(`vehicleIcon()`, SVG `<g transform="rotate(...)">`, круг с номером маршрута не
вращается), добавлен слой живого парка (`/api/live?only_fresh=false`, депо не
показываем) и «машина времени» с режимом «▶ рух» (1 мин модели за 0.9 с, плавная
интерполяция позиции на rAF). Данные и API не менялись. Дизайн стрелок отдан Gemini:
`docs/BRIEF-emulator-vehicles-visual.md`.

**Дизайн маркеров ТС** (`986b21a`): спецспецификация Gemini по брифу выше применена —
круг r=11 с обводкой `route_colour_hex` и тёмной заливкой (белый номер маршрута читается
на любой подложке), треугольник-стрелка в `rotate(a 18 18)`, подпись вне группы,
`popupAnchor [0, -18]`. Станы классом на обёртке: `live` / `planned` (opacity .6 +
grayscale 40%) / `stopped` (speed < 3 — стрелку сховано, обводка пульсует) / `target`
(цель плана — золоте пульсування, z-index 1000). Цель берётся из `leg.live_bus` и
живёт в `state.targetBoards`, а не прапорцем на объекте машины: парк каждые 0.9 с
приходит новыми объектами. Плюс пунктир «як у Google Maps» для пешеходной пересадки
(`leg.path`), когда роутер начнёт её отдавать.

**Легенда карти** (`51374eb`): спека Gemini (п. 2 плану реалізації) — контрол
Leaflet у правому нижньому куті, 4 рядки (живий / за розкладом / ваша посадка /
пішки), CSS з матовим склом над атрибуцією OSM. Клік і свайп по легенді карту не
рухають (`disableClickPropagation`). UI-проверка: 18 → 24 `OK`.

**Попапы, эмуляция безопасности и разгрузка карты** (`5bcff8b`): спека Gemini
(попап + трейл + кластеризация) применена с четырьмя правками, найденными
машинной проверкой: попап → структурированный HTML (`.veh-popup-*` под светлую
тему Leaflet) + `esc()` для `route_label` / `board_number` / `gpstime` (и для
`data-board` в маркере — это атрибут); имя класса бейджа `plan`, а не `planned`
(с несуществующим классом подпись прозрачна на светлом попапе); разгрузка при
отдалении — CSS `body.zoom-out` (`scale(0.65)`, номер и стрелка схованы) без
MarkerCluster, порог `zoom < 13` (`DOT_ZOOM_BELOW`) — карта открывается на 13,
тож номера маршрутов видны сразу; трейл не делаем (истории координат нет).
Плюс `transition: transform .25s ease` на `.veh-wrap`, чтобы сжатие было
плавным. UI-проверка: 24 → 31 `OK`.

**Напрямок ТС у підборі «першого потрібного ТС»** (`docs/BRIEF-router-direction.md`):
ланцюжки A і B ідуть тими самими вулицями, тому зустрічна машина прив'язувалась до
НАШОГО ланцюжка й потрапляла в «під'їжджаючі» — на срезі 17.09 12:00 таких пар
(маршрут, зупинка) було **1149**, і **9 з 30** еталонних планів відрізнялися
(роутер обіцяв живу машину, яка їхала в інший бік). Тепер дві незалежні перевірки:
поле `direction` среза парка (є в симуляторі) і курс ТС проти азимуту «від машини
до наступного вузла» (є й у реальному трекері, який `direction` не віддає). Пороги:
90°, `speed > 5 км/год`, сегмент ≥ 50 м — інакше працює геометричний fallback.
`_FLEET_KEY_FIELDS` отримав `direction`/`heading_deg` (розворот на кінцевій інакше
не скидав би кэш геометрії). Замер: `python tools/perf/probe_direction.py --rev 916c286`
— **1149 → 0** (парк з полем `direction`) і **1149 → 74** (парк без поля; 0/30
планів), `pytest` 10 → **15**, UI **31 OK**.

**Карта плану маршруту** (`docs/REVIEW-map-ux.md`): план Gemini применён с
сокращением объёма — вместо отдельного плагина стрелок и «активной геометрии»
на сервере: `leg.full_geom` (полная геометрия напрямку) + обрезка хвоста на
клиенте (±6 зупинок, `opacity .3`), шеврон напрямку **всередині точки проміжної
зупинки** (один маркер замість двох; окремий шеврон — лише на сегментах > 800 м),
бейдж кроку 30 px у кольорі маршруту з пульсацією, фініш 🏁, іконка 🚶 на пешій
пересадці (геометрії роутер не віддає → пряма між зупинками; раніше пеша нога не
малювалась зовсім), клік по цифрі в панелі → zoom до ноги, `esc()` для назв із API
і легенда з 5-м рядком «Крок плану». Інваріант `path == full_geom[i:j+1]` —
окремий pytest; латентність не змінилась (p50 86.6 → 86.7 мс), `pytest` 15 → **16**,
UI **42 OK**. `compare_snapshots.py` отримав `--ignore-keys` і шлях першої
розбіжності (додане поле інакше назавжди ламало б приймання оптимізацій).

**Гигиена и инструменты:** `7d1eee7` — `.gitignore`, `.dockerignore`,
`.env.example` (его требовал `DEPLOY_EMULATOR.md`, файла не было);
`31d4dcc` — из индекса убран локальный мусор; `1e13bb3` — `tools/perf/`
(замеры и сверки, см. `tools/perf/README.md`); `b51b7e0` — `tools/ui/`
(проверка эмулятора «глазами», см. ниже).

**Разделяй «5» автобус и «5» троллейбус** (правка подбирается к коммиту):
номера маршрутов у автобусов и троллейбусов независимы — «5» есть и там, и
там, и это два разных маршрута (автобус: Держуніверситет → вул. Ентузіастів;
тролейбус: Калинівський ринок → вул. Південно-Кільцева). Роутер же
идентифицировал машину в парке **только по нормализованной подписи маршрута**
(`_live_by_route` с ключом `"5"`), поэтому оба маршрута склеивались в одну
кучу: «перший потрібний ТС» автобусного 5 мог быть троллейбусом 5, на карту
плана автобуса рисовались троллейбусы, а цвет линии прилетал от чужого типа.
Данные были ни при чём (`graph.json`, `routes_schedule.json`, scraped-файлы
корректно разнесены по `bus:`/`trolley:`) — ломалось в коде. Починено:
индекс парка и все привязки к маршруту (`set_live`, `_approaching_vehicles`,
`_vehicles_for_routes`, `_transit_leg`) идут по паре `(тип ТС, подпись)`;
`vehicle_type` вошёл в `_FLEET_KEY_FIELDS` (смена типа машины обязана
сбрасывать кэш геометрии); на фронте ключ маркера и цель подсветки —
`тип|метка|борт`. Тесты: `test_same_number_different_vehicle_type_stays_separate`
и `test_foreign_vehicle_type_is_not_first_bus`. Замер на тех же данных:
`bus:5:A` — было 12 машин типов `['bus','trolley']`, стало 4 типа `['bus']`;
`trolley:5:A` — было 12 смешанных, стало 8 типа `['trolley']`. pytest 16 → 18.

## 3. Быстрые проверки (6 команд)

```powershell
cd c:\Users\YA\.gemini\antigravity\scratch\chernivtsi-stops-editor
python -m pytest -q
node tools\ui\check_emulator.js
python tools\perf\parity_http.py --text-file tools\perf\phrases\soborna_graviton.txt --now 2026-09-17T03:10:00
python tools\perf\check_map_paths.py --text-file tools\perf\phrases\kalynka_universytet.txt --url http://169.58.82.105:8000
python tools\perf\ab_prod_cycle.py --old 1e16f98
```

Ожидаемое: `18 passed`; UI-проверка печатает 42 `OK` и `CHECK_EMULATOR_OK`
(на 2026-09-17: 58–60 маркеров, у всех ненулевой курс, 59 из 59 сменили позицию в
режиме «рух», план рисует линию, `rotate(a) == data-heading`, CSS-правила
`planned`/`stopped`/`target` на месте, подсвечен ровно тот борт, что в тексте
плана («сідати: 6-023» → `target` у 6-023), легенда карти: 5 рядів за
специфікацією, клик и свайп по ней карту не двигают, 129 px из 390 px на
мобильном, попап машини: структура + бейдж непрозрачный в обоих станах +
экранирование внешних строк, зум 12 → точки 23.4 px без номера и стрелки, зум 14
→ полный маркер 36 px, центр маркера не уехал с точки Leaflet (0 px), карта плану:
бейджів кроків = числу поїздок (колір = `leg.color`, 30 px, пульсація), шеврони
напрямку з азимутом із `leg.path` (105 на еталонній фразі; усе із збігом центра
під «туди-назад» рахується як OK), точки проміжних зупинок = `sum(len(path)-2)`,
хвости `opacity 0.3` проти `0.9`, фініш 🏁 один, пунктир пешої ноги `1, 10` +
іконка 🚶 (синтетичний план), цифри в панелі = бейджам, пульсація вимикається на
`prefers-reduced-motion`, консоль
чистая, скриншоты в `tools/ui/out/`); паритет JSON
локально/VPS = `True`; по фразе «з Калінки
до Універу» нога 5 идёт 15 точками (`OK (trolley:5:A)`), «ног с разрывом: 0»;
A/B ≈ 235 → 150 мс (со счётчиками) и haversine 33 168 → 133.

## 4. Грабли (чтобы не наступить снова)

1. `set_live()` **внутри** обработчика запроса (`main.py`) — поэтому кэш
   геометрии сбрасывается не по факту вызова, а по смене подписи позиций.
2. `bench3.py` устарел: счётчики не подключены, `set_live` без `snapshot_at`
   (другой режим, +800 `_wait_info`). Используйте `tools/perf`.
3. Поиск «фраза → остановки» идёт через LLM: без `--now` локальный сервер и VPS
   дают разные планы (разные часы и разный парк симулятора), а кириллица в
   `.cmd`-файле вообще ломается в OEM-кодировке — передавайте `--text-file`.
4. `path` ноги — цепочка остановок; для трассировки по улицам нужны OSM way-линии.
5. UI-проверку (`tools/ui/check_emulator.js`) запускать в headless Chromium, а не
   «глазами по скриншоту»: она ловит и курс, и движение (сравнивает позиции
   маркеров в пикселях), и 404/ошибки консоли. Из `run_commands` вывод длинного
   node-процесса обрезается — гоняйте через `.cmd` в скрытом окне
   (`Start-Process -WindowStyle Hidden`), результат читайте из файла.
6. Поворот стрелки ТС — внутри SVG (`<g class="veh-arrow-group" transform="rotate(a 18 18)">`),
   поэтому круг с номером маршрута и попап остаются прямыми. Не вешайте CSS-`rotate` на
   весь маркер: подписи поедут. Правила трёх станів — в `web/emulator.html`
   (`.veh-wrap.planned/.stopped/.target`), вся вёрстка — по-прежнему в `vehicleIcon()`.
7. Симулятор отдаёт `speed_kmh` = средняя скорость маршрута (~20 км/ч), поэтому
   состояние `stopped` (< 3) на нём не воспроизводится — оно только у реального GPS.
   UI-проверка поэтому тестирует CSS-правила `stopped` на одноразовых элементах, а не
   на живых маркерах: иначе ветку нельзя проверить машинно вообще. `opacity`/`filter`
   не наследуются — читайте их у `.veh-wrap`, а не у вложенного `svg`.
8. `L.DomEvent.disableClickPropagation()` (легенда) **не останавливает DOM-бабблинг
   клика**: Leaflet 1.9 ставит на элемент флаг `_leaflet_disable_click`, который
   игнорирует сам движок карты. Слушать `.leaflet-container` в UI-тесте бессмысленно —
   клик туда долетит. Проверяйте поведение: `map.on('click')` и `map.getCenter()` до/после
   драга (обязательно с контрольным измерением мимо легенды, иначе тест ничего не
   доказывает). Карта доступна из `page.evaluate()` по имени `map` — это `const` верхнего
   уровня в классическом скрипте, а не свойство `window`.
9. Легенда живёт в `web/emulator.html` (`.map-legend`, `.legend-row`, `.legend-icon`),
   разметка — в `LEGEND_ROWS` + `legendIconStyle()` в `web/emulator.js`. Меняете
   подписи/цвета — правьте `LEGEND_ROWS`, а не HTML, иначе UI-тест разойдётся с кодом.
10. Порог «разгрузки» — `DOT_ZOOM_BELOW` в `web/emulator.js`, а карта открывается
    на `zoom 13`: порог `< 14` показывал бы точки сразу при загрузке, поэтому
    режим точек — это `zoom < 13`. Меняете порог — правьте и комментарий у
    `body.zoom-out` в `web/emulator.html`; UI-тест проверяет оба состояния
    (`map.setZoom(12)` и `map.setZoom(14)`), а не классруками.
11. Сжатие маркера — `transform` **на `.veh-wrap` (36 px)**, не на `.veh-marker`:
    Leaflet сам пишет инлайновый `transform: translate3d(...)` на элемент маркера
    (его же читает проверка «машины двигаются»), поэтому CSS-transform там был бы
    перекрыт. А `popupAnchor` и якорь считаются от обёртки — потому точка и попап
    остаются на месте (тест меряет расхождение: 0 px на обоих зумах).
12. **Симулятор «затискає» ніч до середини дня** (`sim_layer._virtual_minutes`):
    поза вікном роботи маршрутів будь-який `now` дає ОДНУ й ту саму позицію
    парку. Через це UI-перевірка «машины двигаются» падала після опівночі
    (`0 из 119`): рух неможливо довести, бо модель часу не рухає парк. Тепер
    `tools/ui/check_emulator.js` сам ставить час моделі (`--model-time`, типово
    `2026-09-17T12:00`) — результат не залежить від годинника машини.
13. **Курс ТС не можна порівнювати з хордою довгого сегмента**: сегменти бувають
    по кілометру, і на кривій хорда розходиться з напрямком дороги до 90° —
    зустрічні машини проходили фільтр. Тому `_heading_matches_route()` бере
    азимут «від машини до наступного вузла» (а азимут сегмента — лише коли
    машина стоїть рівно на вузлі).
14. **`leg.full_geom` — це зупинки напрямку, а не геометрія доріг** (те саме, що
    `path`): хвіст і активна лінія — прямі між зупинками, на довгих перегонах
    виглядають кутово. Інваріант «`path` — неперервний сріз `full_geom`» тримає
    pytest (`test_leg_full_geom_covers_active_path`): якщо він зламається, хвіст
    на карті не зійдеться з лінією (клієнт тоді просто не малює хвіст).
15. **Headless Chromium віддає `prefers-reduced-motion: reduce`** — усі пульсації
    (`pulse-target`, `pulse-stopped`, `pulse-step`) у UI-перевірці вимкнені, поки
    вона явно не попросить `no-preference` через `page.emulateMediaFeatures`.
    Якщо додаєте нову анімацію з `@media (prefers-reduced-motion)`, перевіряйте її
    тим самим способом (і фіксуйте вимкнення окремим ассертом).
16. **Додане поле в план ламає `compare_snapshots.py`** (він порівнює плани
    побайтово): усі 6 еталонних пар стають `IDENTICAL: False`. Це не регресія —
    запускайте з `--ignore-keys <поле>` (список ігнорованих полів і шлях першої
    розбіжності друкуються в звіті), інакше ускорення буде нечем прийняти.
17. **Номера маршрутів у автобусів і тролейбусів НЕ перетинаються.** «5» —
    це одразу два маршрути (автобус і тролейбус) з різними ланцюжками, і так
    само 1/3/4/6/8. Тому будь-яка прив'язка «парк ↔ маршрут» ідентифікує
    маршрут **парою (тип ТС, підпис)**, а не лише підписою: індекс
    `_live_by_route` у `set_live()`, вибірка кандидатів у `_approaching_vehicles()`,
    список ТС плану у `_vehicles_for_routes()`, колір ноги у `_transit_leg()`.
    За голої підписи вони злипались би в один ключ `"5"` — і «перший потрібний
    ТС» автобусного 5 ставав тролейбусом 5. Те саме на фронті: ключ маркера і
    ціль підсвічування — `тип|метка|борт` (`vehicleKey`/`boardKey` у
    `web/emulator.js`), бо бортові номери теж можуть збігатися між типами.
    Додаєте нове місце зіставлення парку з маршрутом — одразу пишіть про тип
    у коментарі, а краще одразу покривайте його тестом
    (`test_same_number_different_vehicle_type_stays_separate`).
18. **Шеврони напрямку: в `leg.path` трапляються ділянки «туди-назад».** Маршрут
    може проходити одну й ту саму дорогу в обидва боки (виїзд/петля), тоді у
    сегментів `A→B` і `B→A` **однаковий центр**, але азимути розходяться на 180°.
    Перевірка `check_emulator.js` (`expectedBearings`) збирає **усіх** кандидатів
    із таким центром і засчитовує збіг із будь-яким — чий азимут збігається, той
    сегмент і намальовано. Не поверніть її до «першого збігу»: на плані
    «Соборка → Гравітон» (bus 9A) це давало 2 ложних провали з 105 шевронів
    (want 155.7 vs got 335.7 — різниця рівно 180°). Якщо хочете перевіряти ще й
    візуальний `transform: rotate()` шеврона — беріть `data-bearing`, він
    незмінно фіксує азимут сегмента (`midChevron` у `web/emulator.js`).



## 5. Открытые задачи

1. **HTTPS + basic auth** — конфиги готовы в `deploy/` (`nginx-emulator.conf`,
   `nginx-emulator-http.conf`, `setup-https.sh`, `README_HTTPS.md`), порт `8000`
   уже закрыт наружу биндом `127.0.0.1:8000:8000`. Осталось на сервере:
   прописать домен (A-запись → `169.58.82.105`) и выполнить
   `sudo bash deploy/setup-https.sh <домен> <email>` (htpasswd + certbot + reload).
2. **Ротация ключа OpenRouter** (ключ ранее светился в открытом виде).
3. **Геометрия дорог на карте**: OSM way-линии вместо прямых между остановками.
4. `docs/DECISIONS.md` / `AGENTS.md`: правило «писатель ≠ проверяющий»,
   порядок бриф (Gemini) → реализация (Cline) → разбор (Claude/Cline).
5. **Легенда: свотч vs реальный маркер** (`51374eb` легенда уже есть): её точки —
   концепт стану (зелена/сіра/золота), а живі маркери на карті залиті кольором
   маршруту, тож зелений кружечок не дорівнює кольору машини. Якщо Gemini хоче
   буквальну відповідність — замінити `●` на міні-SVG справжнього маркера
   (круг + стрілка), це один рядок у `LEGEND_ROWS`.
6. З брифу `docs/BRIEF-emulator-vehicles-visual.md` питання 7–9 закрито в `5bcff8b`
   (попап — структурований HTML + `esc()`, трейл — свідомо ні, розвантаження — CSS
   при `zoom < 13`), відповіді зафіксовано в §10 брифа. Лишилось дрібне на розсуд
   дизайну: `popupAnchor [0, -18]` при стисненні дає попап на ~6 px вище; підпис
   бейджа («GPS»/«Розклад») проти легенди («Живий (GPS)»/«За розкладом»); на 390 px
   крапка 23 px — запасу для пальця мало (`scale(0.8)` у `@media` дасть 29 px).
7. **Прив'язка ТС до ланцюжка: 600 м — занадто щедро.** Після фільтра напрямку
   (`BRIEF-router-direction.md`) на парку БЕЗ поля `direction` лишається 74
   «зустрічних» кандидати — і всі з прив'язкою 300+ м: там і курс, і `eta`
   випадкові (латеральне зміщення перекриває напрямок дороги). Жоден з них не
   змінює жодного плану на еталонному наборі, але це варто заміряти окремо
   (`MAX_LIVE_SNAP_METERS` 600 → 150–250) разом із `tools/perf/latency_plan.py`.
8. **Пеша геометрія пересадки** (`docs/REVIEW-map-ux.md` §1.4): роутер не віддає
   `leg.path` для пеших ніг, тому пересадка малюється прямою між зупинками
   (клієнтський фолбэк). Прокласти пішки по графу — і фолбэк стане непотрібним.
   Дрібне поруч: при `zoom < 13` бейджі кроків і шеврони не сжимаються
   (`.zoom-out` діє лише на маркери ТС) — вирішити з дизайном.

## 6. Если начинаете новую сессию

Контекст специально вынесен в файлы, чтобы стартовая сессия ничего не теряла.
Скопируйте агенту такое вступление:

> Прочитай `docs/STATUS.md`, `docs/REVIEW-router-latency.md`,
> `docs/REVIEW-router-perf.md`, `docs/REVIEW-router-correctness.md`,
> `docs/REVIEW-router-map-paths.md`, `tools/perf/README.md` и заголовок
> `tools/ui/check_emulator.js`.
> Правило проекта: **писатель ≠ проверяющий** — бриф пишет один (Gemini),
> реализует другой (Cline), проверяем машинно (`python -m pytest -q`,
> `node tools/ui/check_emulator.js`, `tools/perf/compare_snapshots.py`,
> `tools/perf/ab_prod_cycle.py`), и только
> потом коммит → push → деплой на VPS.
> Задача: <что делаем>.

Служебное: рабочие файлы прошлых сессий лежали в `C:\Temp` (скрипты, слепки,
ssh-обвязка деплоя `remote_update.sh`, `.cmd` для внешних проверок) — их можно
удалять, всё значимое перенесено в `tools/perf/`, `tools/ui/` и `docs/`.
