# 🚌 Емулятор маршрутів із живим GPS — план

> **Створено:** 2026-09-14 (спільна сесія)
> **Мета:** веб-емуляторі + «мозок» для розрахунку маршруту «Соборка → Гравітон» з пересадками, тарифом і реальними ТС (GPS). Потім — інтеграція у нативну RN-апку TransGPS.

---

## 1. Сценарій користувача

1. Користувач (на смартфоні) говорить або пише: **«Я на Соборці, їду на Гравітон»**.
2. Система з розуміє: `from = Соборка`, `to = Гравітон`.
3. Якщо «звідки» незрозуміло — запитуємо геолокацію (GPS смартфона) → найближчі зупинки.
4. На карті малюється **1–2 маршрути з пересадкою**: лінії 1️⃣ і 2️⃣, бейджі маршрутів, маркер пересадки ⇄, старт.
5. Показуються: **час очікування + час у дорозі + вартість + бортовий номер ТС**.
6. Вибір оптимального варіанта враховує **реальне положення ТС по GPS**:
   «якщо автобус під'їде раніше за тролейбус — видаємо автобус».

---

## 2. Архітектура (три «поверхи мозку»)

```
Телефон (веб-емулятор / RN-апка)          Сервер (локалка → VPS)
┌──────────────────────────┐              ┌───────────────────────────────────┐
│ 🎤 мікрофон / текст       │              │ 1) LLM — розуміє фразу → from/to   │
│ 📍 геолокація            │ ←──REST──►   │ 2) Locator (RapidFuzz) — назва→    │
│ 🗺 Leaflet / RN Maps      │  (JSON)      │    зупинка (аліаси, «Соборка»)     │
│   Polyline 1️⃣/2️⃣         │              │ 3) Статичний граф маршрутів:        │
│   маркери, тариф, ETA    │              │    scraped_data + osm_stops.json   │
└──────────────────────────┘              │ 4) Живий шар GPS: реальні ТС       │
                                          │    → ETA на зупинку посадки        │
                                          │ 5) Тарифна модель (конфіг)         │
                                          └───────────────────────────────────┘
```

**Принцип:** LLM/нев. НЕ вважає маршрути — він лише розуміє запит. Маршрут і пересадки рахує **алгоритмічний граф** (детерміновано, мілісекунди). Живий GPS шар вибирає «який саме ТС сісти».

### Дані для графа
- Вершини: зупинки (канонічні вузли з `osm_stops.json` + `scraped_data`)
- Ребра: послідовність зупинок уздовж маршруту (bus/trolley, напрямок A/B), вага = хвилини (~2 хв на зупинку)
- Пересадка: з'єднання зупинок по координатах (радіус ~80–100 м) + fuzzy-збіг назв
- Пошук: BFS із обмеженням на **1–2 пересадки**; найкращий = мін. (очікування + дорога + штраф за пересадку)

### Живий GPS шар
- Джерело: **`https://trans-gps.cv.ua/`** (власний сайт користувача, реальні ТС) — ендпоінт ще треба розвідати
- Альтернатива для тесту: власний симулятор віртуальних ТС (рух по маршрутах)
- ETA: реальна дистанція + швидкість + кількість зупинок до посадки; оновлення через WebSocket (5–15 с)
- Фолбек: якщо живого ТС немає — статичний розклад (`routes_schedule.json`)

### Тариф (конфіг, легко змінюється)
```json
{ "bus_price": 10, "trolley_price": 5, "transfer_window_min": 40 }
```

---

## 3. Контракт API (приклад відповіді POST /api/plan)

```json
POST /api/plan
{ "from": "Соборка", "to": "Гравітон", "my_pos": {"lat":48.2917,"lon":25.9346} }

{
  "legs": [
    { "type": "transit", "vehicle": "bus", "route": "23",
      "from": "Соборна площа", "to": "Центральний ринок",
      "color": "#1e3a8a", "path": [[48.291,25.935],["48.29,25.94"]],
      "wait_min": 4, "travel_min": 12, "live_bus": "Б357КС", "eta": "14:41" },
    { "type": "transfer", "at": "Центральний ринок", "wait_min": 2 },
    { "type": "transit", "vehicle": "trolley", "route": "4",
      "from": "Центральний ринок", "to": "Завод «Гравітон»",
      "color": "#d97706", "path": [...], "wait_min": 7, "travel_min": 8,
      "live_bus": "Т12", "eta": "14:50" }
  ],
  "total_min": 26, "price_grn": 15, "transfers": 1, "alternative": []
}
```

**RN-інтеграція:** `fetch` + `react-native-maps` `Polyline` — нічого екзотичного.

---

## 4. Що вже зроблено (2026-09-14)

- ✅ Жирна сплошна лінія маршруту на карті (weight 6, без dashArray)
- ✅ Палітра кольорів 🎨 (12 кольорів, localStorage окремо на кожен маршрут)
- ✅ Мультиселект маршрутів: чекбокси ліній + прапорець «показати зупинки»
  (`stopsVisibleByType`) — статичні маркери інших маршрутів у колір лінії
- ✅ Фікс показу/приховування панелі маршрутів (було: відсутній `.hidden` для панелі;
  тепер: панель у потоці сайдбара, 3 колонки, не накриває решту)
- ✅ Розклад: `routes_schedule.json` (маршрут → день → час)

---

## 5. Етапи реалізації (кожен — «можна тицьнути на телефоні»)

| # | Етап | Результат |
|---|------|-----------|
| 0 | HTTPS-доступ до локалки зі смартфона (самопідписаний сертифікат) | редактор відкривається на телефоні |
| 1 | Моб. екран емулятора: поле «Звідки → Куди» + кнопка 🎤 | текст фрази на екрані |
| 2 | Алгоритмічний роутер: «Соборка → Гравітон» → 2 лінії + тариф | main demo |
| 3 | Геолокація: якщо «звідки» не сказано — точки GPS + найближчі зупинки | автозаповнення from |
| 4 | Голос (Web Speech) + озвучка відповіді (TTS) | повний «голосовий» досвід |
| 5 | Розвідка API `trans-gps.cv.ua` → GPS-шлюз + WebSocket живого руху | реальні ТС і ETA |
| 6 | Деплой движка на VPS (Contabo) + інтеграція в RN-апку | прод |

---

## 6. Відкриті питання

- [ ] Ендпоінт позицій ТС на `trans-gps.cv.ua` (розвідати JS сайту)
- [ ] Скільки пересадок допускаємо: 1 чи 2?
- [ ] Точні цифри тарифу (COVID період), вікно пересадки
- [ ] Час показувати лише у вигляді «через N хв» чи ще точний час прибуття?
- [ ] Фолбек на розклад: залишити чи вимкнути?

---

## 7. Посилання

- Сайт із реальними ТС: https://trans-gps.cv.ua/
- Апка: https://play.google.com/store/apps/details?id=com.companyname.TransGps (React Native)
- План AI/VPS: `AI_SERVER_PLAN.md`