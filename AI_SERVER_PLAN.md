# 🤖 План: ИИ на сервер + маршрутизация TransGPS

> **Создано:** 2026-09-07 (сессия с Cline)
> **Статус:** план согласован, установка не начата

---

## 1. Цель

Поставить локальную LLM для приложения **TransGPS: Транспорт Чернівці**:
- просчёт маршрутов и пересадок по запросам пользователей
- понимание народных названий остановок («Калинка», «Соборка», «Універ», «Гравітон»)
- помощь в админке digital-signage
- в перспективе: голосовой ввод (Whisper)

**Ключевой принцип:** ИИ НЕ считает маршруты сам — он понимает запрос и вызывает
алгоритмический роутер (данные остановок из `chernivtsi-stops-editor`).

```
Запрос юзера → LLM (понимание А→Б, извлечение JSON)
            → Routing Engine (граф маршрутов из scraped_data)
            → LLM (человекочитаемый ответ)
```

---

## 2. Сервер (VPS Contabo)

- **IP:** 169.58.82.105 (регион EU)
- **Конфиг:** 4 vCPU / 8 GB RAM / 100 GB SSD / без GPU
- **ОС:** Linux, пользователь SSH: `cont` → `sudo su -`
- **Уже крутится прод:** digital-signage (Node.js + SQLite, порт 3001, PM2)

### Решение по размещению ИИ (выбран гибрид):

1. **Сейчас:** поставить Ollama на текущий VPS — только как СТЕНД для промптов
   - привязать строго к `127.0.0.1:11434`
   - systemd-лимит `MemoryMax=3G` → OOM-killer убьёт Ollama, а не прод
   - `OLLAMA_MAX_LOADED_MODELS=1`
2. **Если пойдёт в прод / для пассажиров:** арендовать ОТДЕЛЬНЫЙ VPS под ИИ
   (4–8 vCPU / 16–30 GB RAM, ~€10–15/мес) — там влезет Qwen 2.5-14B
3. Снос = чисто: `systemctl disable --now ollama` + удалить папку моделей

### Модель:

- **Стенд на текущем VPS:** `qwen2.5:3b` (Q4, ~2.2 GB RAM, 18–25 ток/с на CPU)
- **7B/14B на этом сервере НЕ влезут** (8 GB заняты продом)
- Причина выбора Qwen: лучший украинский язык в open-source + Function Calling

### Установка (шаги):

```bash
ssh cont@169.58.82.105
sudo su -
curl -fsSL https://ollama.com/install.sh | sh
sudo systemctl edit ollama          # Environment="OLLAMA_HOST=127.0.0.1:11434"
                                     # Environment="OLLAMA_MAX_LOADED_MODELS=1"
                                     # + MemoryMax=3G в [Service]
sudo systemctl restart ollama
ollama run qwen2.5:3b
free -h                              # проверить память до/после
```

API: `http://localhost:11434/v1/chat/completions` (OpenAI-совместимый)

---

## 3. Данные остановок — ГОТОВО (2026-09-07)

Проект `chernivtsi-stops-editor`, коммит `cb97c69` (не запушен в origin):

- ✅ Удалены ~50 иногородних файлов (Киев, Днепр, Львов, межсити 123/227/801...)
- ✅ Починен баг парсера имён: eway добавил префикс `[stop_id]`,
  старый `split('[')` отдавал пустоту → regex `^\[\d+\]\s*`
- ✅ Автобус №8: был поезд «Чернівці—Київ» (коллизия номера с межсити id 1591000119),
  теперь настоящий маршрут Садова → м/н Клокучка (eway id **21**)
- ⚠️ Троллейбус №8 = eway id **75** (Держуніверситет — Південно-Кільцева) — не путать!
- ✅ Добавлено 7А (Садова → Світанок, 20 остановок, eway id 55); 7Б существовал
- ✅ 15, 15К, 7Б, все троллейбусы сверены с eway — идентичны
- ✅ Финальный аудит: 166 файлов, 0 проблем
- 🗺 Начата OSM-интеграция (osm_stops.json, overpass_test.js)

### Полезные инструменты проекта:

- `audit_routes.js` — аудит всех маршрутов (иногородние, координаты, пустые имена)
- `scrape_routes_list.js` — официальная карта eway «ID → номер → тип»
  (Тролейбусы: 1,2,3,4,5(=id8),6(=id7),6A(=id6),8(=id75))
- `scrape_fix.js` — парсер маршрутов с фиксом префикса имён
- `serve.js` — локальный сервер редактора (порт 8080), пароль 15201520

---

## 4. Следующие шаги (дорожная карта)

1. [ ] Запушить `cb97c69` в origin/main
2. [ ] Установить Ollama на VPS (по шагам из п.2) — проверить память
3. [ ] Тест промптов: извлечение «откуда → куда» из фраз на украинском
4. [ ] Собрать словарь народных названий остановок (RAG / synonyms)
5. [ ] Tool: `GET /stops?q=` → ID остановки → поиск пересадок через граф scraped_data
6. [ ] Решение: отдельный VPS под ИИ (для пассажирского чат-бота — ОБЯЗАТЕЛЬНО)
7. [ ] Позже: Whisper (голос), GTFS/OpenTripPlanner для алгоритмического роутинга

### Открытые вопросы:

- Два незакоммиченных файла в stops-editor: `debug_stop.js`, `test_routes.js`
- Локальный сервер редактора (serve.js, порт 8080) мог остаться запущенным
